name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: npm install npm@latest -g
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn
    - name: Run tests
      run: yarn test
